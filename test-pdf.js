const fs = require('fs');
const pdf = require('pdf-parse');

async function test() {
  // Let's assume the PDF for AGUAS CLARAS is available or we can find one.
  // Is there a PDF we can parse? Let's check the folders.
}
test();
